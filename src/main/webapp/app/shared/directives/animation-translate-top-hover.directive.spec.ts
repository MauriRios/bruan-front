import { AnimationTranslateTopHoverDirective } from './animation-translate-top-hover.directive';

describe('AnimationTranslateTopHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationTranslateTopHoverDirective();
    expect(directive).toBeTruthy();
  });
});
