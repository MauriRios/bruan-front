import { FilterByPropertiesPipe } from './filter-by-properties.pipe';

describe('FilterByPropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
